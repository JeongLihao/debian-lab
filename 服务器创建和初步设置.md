# 1. 创建 VPS

在 Vultr 创建实例时，选择：

```text
Shared CPU
Tokyo
Debian 12 x64
1 vCPU
2GB RAM
55GB SSD
```

关闭：

```text
Automatic Backups
```

---

# 2. Windows 生成 SSH Key

打开 CMD 或 PowerShell：

```powershell
ssh-keygen
```

一路回车。

默认会生成：

```text
C:\Users\Administrator\.ssh\
```

目录中的：

```text
id_ed25519
id_ed25519.pub
```

其中：

| 文件           | 作用                 |
| -------------- | -------------------- |
| id_ed25519     | 私钥                 |
| id_ed25519.pub | 公钥（上传到服务器） |

---

# 3. 查看 SSH 公钥

CMD：

```cmd
type %USERPROFILE%\.ssh\id_ed25519.pub
```

PowerShell：

```powershell
type $env:USERPROFILE\.ssh\id_ed25519.pub
```

输出类似：

```text
ssh-ed25519 XXXXXXXXXXXXXX
```

将整行复制到 Vultr SSH Keys。

---

# 4. SSH 登录 VPS

部署完成后：

```powershell
ssh root@服务器IP
```

首次连接会提示：

```text
Are you sure you want to continue connecting
```

输入：

```text
yes
```

---

# 5. 更新系统

```bash
apt update && apt full-upgrade -y
```

---

# 6. 安装基础工具

```bash
apt install -y \
git curl wget vim htop btop \
ufw fail2ban
```

---

# 7. 创建普通用户

创建用户：

```bash
adduser winston
```

加入 sudo：

```bash
usermod -aG sudo winston
```

---

# 8. 配置防火墙（UFW）

允许 SSH：

```bash
ufw allow OpenSSH
```

启用防火墙：

```bash
ufw enable
```

查看状态：

```bash
ufw status
```

---

# 9. SSH 安全加固

编辑 SSH 配置：

```bash
nano /etc/ssh/sshd_config
```

修改：

```text
PermitRootLogin prohibit-password
PasswordAuthentication no
PubkeyAuthentication yes
```

含义：

| 配置                              | 作用                     |
| --------------------------------- | ------------------------ |
| PermitRootLogin prohibit-password | root 仅允许 SSH Key 登录 |
| PasswordAuthentication no         | 禁止密码登录             |
| PubkeyAuthentication yes          | 启用公钥认证             |

---

# 10. SSH 配置检查

检查 SSH 配置是否有语法错误：

```bash
sshd -t
```

如果没有输出：

```text
说明配置正确
```

---

# 11. 重启 SSH 服务

Debian 12：

```bash
systemctl restart ssh
```

---

# 12. 验证 SSH 配置

查看 PasswordAuthentication：

```bash
sshd -T | grep passwordauthentication
```

查看 PermitRootLogin：

```bash
sshd -T | grep permitrootlogin
```

---

# 13. 遇到的问题：配置未生效

虽然：

```text
/etc/ssh/sshd_config
```

已经设置：

```text
PasswordAuthentication no
```

但：

```bash
sshd -T | grep passwordauthentication
```

仍然输出：

```text
passwordauthentication yes
```

---

# 14. 排查配置覆盖

执行：

```bash
grep -R PasswordAuthentication /etc/ssh
```

发现：

```text
/etc/ssh/sshd_config.d/50-cloud-init.conf
```

存在：

```text
PasswordAuthentication yes
```

覆盖了主配置。

---

# 15. cloud-init 配置修复

编辑：

```bash
nano /etc/ssh/sshd_config.d/50-cloud-init.conf
```

修改：

```text
PasswordAuthentication no
```

然后：

```bash
sshd -t
systemctl restart ssh
```

再次检查：

```bash
sshd -T | grep passwordauthentication
```

输出：

```text
passwordauthentication no
```

说明密码登录已彻底关闭。

---

# 16. 验证密码登录是否失效

本地执行：

```powershell
ssh -o PubkeyAuthentication=no root@服务器IP
```

如果返回：

```text
Permission denied
```

说明：

```text
密码登录已关闭
```

---

# 17. Linux 配置覆盖机制

现代 Linux 系统很多服务都会：

```text
主配置
+
conf.d 覆盖目录
```

常见：

| 服务    | 覆盖目录       |
| ------- | -------------- |
| SSH     | sshd_config.d  |
| nginx   | conf.d         |
| systemd | systemd/*.d    |
| apt     | sources.list.d |

因此：

```text
改了配置但没生效
```



# 18. 常用命令备忘

## 查看日志

```bash
journalctl -xe
```

## 查看服务日志

```bash
journalctl -u ssh
```

## 查看端口监听

```bash
ss -tulpn
```

## 查看进程

```bash
ps aux
```

## 查看 IP

```bash
ip a
```

## 查看防火墙状态

```bash
ufw status
```

## 查看系统资源

```bash
htop
```

